# Supabase 接続設定
# このファイルをコピーして .env.local を作成し、開発環境用の値を設定してください
# 本番環境の設定は GitHub Secrets で管理します

# === フロントエンド用（ブラウザで使用） ===
# Supabase プロジェクトURL
PUBLIC_SUPABASE_URL=https://your-dev-project-id.supabase.co

# Supabase 匿名キー（公開しても安全）
PUBLIC_SUPABASE_ANON_KEY=your-dev-supabase-anon-key-here

# モックAPI使用フラグ（開発用）
# true: モックデータを使用（Supabaseに接続しない）
# false または未設定: 実際のSupabase APIを使用
PUBLIC_USE_MOCK_API=false

# === Supabase CLI用（サーバーサイドのみ） ===
# アクセストークン
# 取得方法: Supabaseダッシュボード > Settings > Access Tokens
SUPABASE_ACCESS_TOKEN=your-dev-access-token-here

# プロジェクトID（Reference ID）
# 取得方法: Supabaseダッシュボード > Settings > General > Reference ID
SUPABASE_PROJECT_ID=your-dev-project-id

# === 本番環境の設定 ===
# 本番環境の値は GitHub Secrets で以下を設定してください:
# - PUBLIC_SUPABASE_URL (本番用URL)
# - PUBLIC_SUPABASE_ANON_KEY (本番用匿名キー)
# - SUPABASE_ACCESS_TOKEN (本番用アクセストークン)
# - SUPABASE_PROJECT_ID (本番用プロジェクトID)
